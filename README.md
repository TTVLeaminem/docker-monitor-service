# Docker Container Monitor

Сервис мониторинга Docker контейнеров с уведомлениями в Telegram.

## Возможности

- Мониторинг состояния Docker контейнеров в реальном времени
- Уведомления в Telegram о изменениях статуса контейнеров
- Отслеживание через Docker Events для мгновенных уведомлений
- Периодическая проверка состояния контейнеров
- Управление через Telegram бота с кнопками

## Требования

- Docker и Docker Compose
- Python 3.11+
- Telegram Bot Token
- Telegram Chat ID

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd docker-monitor-service
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Заполните переменные окружения в `.env`:
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `TELEGRAM_CHAT_ID` - ID чата для уведомлений
- `MONITOR_INTERVAL` - интервал проверки в секундах (по умолчанию 60)
- `MONITORED_CONTAINERS` - список контейнеров для мониторинга (через запятую, пустое значение = все контейнеры)

4. Запустите сервис:
```bash
docker compose up -d
```

## Использование

После запуска бот будет автоматически отслеживать изменения в контейнерах и отправлять уведомления в указанный чат.

### Команды Telegram бота

- `/start` - Начать работу с ботом
- Кнопка "📋 Список контейнеров" - показать список всех контейнеров
- Кнопка "📊 Статус контейнеров" - показать статус всех контейнеров

## Структура проекта

```
.
├── main.py              # Основной код мониторинга
├── Dockerfile           # Docker образ
├── entrypoint.sh        # Скрипт запуска
├── requirements.txt     # Python зависимости
├── docker-compose.yml   # Docker Compose конфигурация
└── README.md           # Документация
```

## Переменные окружения

| Переменная | Описание | Обязательная | По умолчанию |
|-----------|----------|--------------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | Да | - |
| `TELEGRAM_CHAT_ID` | ID чата для уведомлений | Да | - |
| `MONITOR_INTERVAL` | Интервал проверки (секунды) | Нет | 60 |
| `MONITOR_STATE_FILE` | Путь к файлу состояния | Нет | /tmp/docker-monitor-state.json |
| `MONITORED_CONTAINERS` | Список контейнеров (через запятую) | Нет | все контейнеры |

## Логи

Просмотр логов:
```bash
docker compose logs -f monitor
```

## Остановка

```bash
docker compose down
```

## Лицензия

MIT

